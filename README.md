# Портфолио студента

Одностраничный сайт-портфолио на Laravel с минималистичным серым дизайном.

## Особенности

- ✨ Минималистичный серый дизайн
- 📸 Загрузка фотографий работ в storage
- 📝 Описание каждой работы
- 🗑️ Удаление работ из портфолио
- 🐳 База данных MySQL в Docker

## Требования

- PHP 8.3+
- Composer
- Docker Desktop
- MySQL (в Docker контейнере)

## Установка и запуск

### 1. База данных

Запустите MySQL в Docker:

```bash
docker-compose up -d
```

### 2. Миграции

Выполните миграции базы данных:

```bash
php artisan migrate
```

### 3. Storage

Создайте символическую ссылку для доступа к файлам:

```bash
php artisan storage:link
```

### 4. Запуск сервера

Запустите встроенный сервер Laravel:

```bash
php artisan serve
```

Сайт будет доступен по адресу: `http://localhost:8000`

## Использование

### Главная страница
- Откройте `http://localhost:8000` для просмотра портфолио
- Все работы отображаются в виде сетки карточек

### Добавление работы
1. Нажмите кнопку "Добавить работу"
2. Заполните форму:
   - Название работы
   - Описание проекта
   - Загрузите изображение (JPEG, PNG, JPG, GIF, до 10 МБ)
3. Нажмите "Опубликовать"

### Удаление работы
- Нажмите кнопку "Удалить" на карточке работы
- Подтвердите удаление

## Структура проекта

```
portfolio/
├── app/
│   ├── Http/Controllers/
│   │   └── PostController.php    # Контроллер для работы с постами
│   └── Models/
│       └── Post.php               # Модель поста
├── database/
│   └── migrations/
│       └── *_create_posts_table.php  # Миграция для таблицы постов
├── resources/
│   └── views/
│       ├── portfolio.blade.php    # Главная страница
│       └── posts/
│           └── create.blade.php   # Форма добавления работы
├── routes/
│   └── web.php                    # Маршруты приложения
├── storage/
│   └── app/
│       └── public/
│           └── posts/             # Хранилище изображений
├── docker-compose.yml             # Конфигурация Docker
└── .env                           # Настройки окружения
```

## Технологии

- **Backend**: Laravel 12
- **Database**: MySQL 8.0
- **Container**: Docker
- **Storage**: Laravel Storage (local)
- **Frontend**: Blade templates, чистый CSS

## Конфигурация базы данных

База данных настроена в файле `.env`:

```
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=portfolio
DB_USERNAME=portfolio_user
DB_PASSWORD=portfolio_password
```

## Остановка проекта

Для остановки MySQL контейнера:

```bash
docker-compose down
```

## Лицензия

Этот проект создан в образовательных целях.
